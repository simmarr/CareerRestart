# Day 25

## DSA

### LeetCode 167 - Two Sum II
Pattern:two pointers
Key idea:in sorted array ,use two pointers to skip using O(n) space.
Time:O(n)
Space:O(1)

### LeetCode 15 - 3Sum
Pattern:sorting and two pointer
Key idea:sort the array,fix an element,and iterate over rest of array with two pointer
Time:O(n*n)
Space:O(1)

## Core CS - Computer Networks

### TCP vs UDP
TCP:connection ,reliable,delivery matters
UDP:connectionless,low latency

Key difference:tcp is used for correctness while udp for smoothness

## Web Development

Today I learned:

- HTML:structure of the frontend(skeleton)
- CSS:styles the structure
- JavaScript:decides the behaviour of structure
- DOM: representation of the HTML document that JavaScript can access and modify
- Event Listener: waits for an event like a click and runs a function when it happens

## Biggest Takeaways

1.sorting helps to use two pointers
2.tcp -reliability  udp-low latency
3.through DOM